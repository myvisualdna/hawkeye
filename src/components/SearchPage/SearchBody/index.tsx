import React from 'react';
import SearchGridHasMap from 'src/components/SearchPage/SearchBody/searchGridHasMap'

type Props = {};

const SearchBody = (props: Props) => {
  return (
    <div>
      <SearchGridHasMap />
    </div>
  );
};

export default SearchBody;
